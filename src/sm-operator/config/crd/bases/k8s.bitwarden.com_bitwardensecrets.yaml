---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.12.0
  name: bitwardensecrets.k8s.bitwarden.com
spec:
  group: k8s.bitwarden.com
  names:
    kind: BitwardenSecret
    listKind: BitwardenSecretList
    plural: bitwardensecrets
    singular: bitwardensecret
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: BitwardenSecret is the Schema for the bitwardensecrets API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: BitwardenSecretSpec defines the desired state of BitwardenSecret
            properties:
              authToken:
                description: The secret key reference for the authorization token
                  used to connect to Secrets Manager
                properties:
                  secretKey:
                    description: The key of the secret where the authorization token
                      is stored
                    type: string
                  secretName:
                    description: The name of the secret where the authorization token
                      is stored
                    type: string
                required:
                - secretKey
                - secretName
                type: object
              bitwardenApiUrl:
                description: The base URL for Secrets Manager API.  When in doubt,
                  leave blank
                type: string
              identityApiUrl:
                description: The base URL for the Bitwarden Identity API.  When in
                  doubt, leave blank
                type: string
              organizationId:
                description: The organization ID for your organization
                type: string
              projects:
                description: The projects to sync from the organization
                items:
                  properties:
                    excludedSecretIds:
                      description: Optional filter to specify that these secrets should
                        not be included in the secrets created
                      items:
                        type: string
                      type: array
                    includedSecretIds:
                      description: Optional filter to specify that only these IDs
                        are included in the secrets created
                      items:
                        type: string
                      type: array
                    map:
                      description: The mapping of project secrets to K8s secret keys
                      items:
                        properties:
                          bwSecretId:
                            description: The ID of the secret in Secrets Manager
                            type: string
                          secretKeyName:
                            description: The name of the mapped key in the created
                              Kubernetes secret
                            type: string
                        required:
                        - bwSecretId
                        - secretKeyName
                        type: object
                      type: array
                    projectId:
                      description: The Project ID for your organization
                      type: string
                    secretName:
                      description: The name of the secret for the
                      type: string
                  required:
                  - map
                  - projectId
                  - secretName
                  type: object
                type: array
            required:
            - authToken
            - organizationId
            - projects
            type: object
          status:
            description: BitwardenSecretStatus defines the observed state of BitwardenSecret
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
